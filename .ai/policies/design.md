# Existing design policy

- Preserve and extend the existing visual language unless the request explicitly changes the design.
- Inspect nearby pages, components, tokens, spacing, typography, colors, and interaction patterns before implementing UI.
- Reuse existing components and design tokens. Do not introduce a parallel design system.
- Keep deliberate differences only when the user request or product semantics require them.
